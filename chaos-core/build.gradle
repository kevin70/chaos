publishing {
  publications {
    mavenJava(MavenPublication) {
      pom {
        description = "Yein Chaos Core"
      }
    }
  }
}

dependencies {
  api("com.google.guava:guava:${guavaVersion}")
}
